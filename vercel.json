{
    "version": 2,
    "rewrites": [{ "source": "/(.*)", "destination": "/api" }],
    "builds": [{
        "src": "src/api/index.ts",
        "use": "@vercel/node"
    }],
    "routes":[
        {
            "src":"(.*)",
            "dest": "src/api/index.ts"
        }
    ]
}